# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password
DB_DATABASE=spot_optimizer
DB_POOL_SIZE=10

# Application Configuration
FLASK_ENV=development
FLASK_DEBUG=True
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# Security Configuration
ADMIN_TOKEN=change-this-admin-token-in-production
CORS_ORIGINS=*
SECRET_KEY=change-this-secret-key-in-production

# Paths Configuration (optional - defaults to subdirectories)
# DECISION_ENGINE_DIR=./decision_engines
# MODEL_DIR=./models
# LOG_DIR=./logs

# Logging Configuration
LOG_LEVEL=INFO

# Agent Configuration
AGENT_HEARTBEAT_TIMEOUT=120
DEFAULT_TERMINATE_WAIT_SECONDS=300

# Background Jobs
ENABLE_BACKGROUND_JOBS=True
